package com.example.interfaces;

public interface Speakers {

    public String makeSound();
}

/**
    cannot use annotation above interfaces as they cannot be beans.
    beans can only be created from java classes.
 */
